import { Typography } from '@mui/material';
import React from 'react';

const Pay = () => {
    return (
        <div>
            <Typography variant="h4">Payment Method Coming Soon!</Typography>
        </div>
    );
};

export default Pay;